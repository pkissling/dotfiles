#!/bin/sh

# from https://medium.com/@joaomoreno/persistent-terminal-sessions-in-vs-code-8fc469ed6b41
SESSION=$(echo "vscode-$(pwd)" | sed 's/\./_/g')
"${HOMEBREW_PREFIX}"/opt/tmux/bin/tmux attach-session -t "$SESSION" || "${HOMEBREW_PREFIX}"/opt/tmux/bin/tmux new-session -s "$SESSION"
