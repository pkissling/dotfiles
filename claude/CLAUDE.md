# Workflow

After completing any task that changed code, run the `/simplify` skill to review the diff for reuse, quality, and efficiency issues before reporting the task as done. Skip this for tasks that did not modify code (questions, research, config-only changes that don't touch source).
